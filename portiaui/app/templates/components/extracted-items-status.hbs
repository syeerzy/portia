{{#link-to changeInfo.path changeInfo.model bubbles=false}}
    {{#help-icon icon=icon placement='left'}}
        {{#if hasWarning}}
            {{changeInfo.text}}
        {{else}}
            设置样本配置为抽取
        {{/if}}
    {{/help-icon}}
{{/link-to}}
