{{#list-item-relation-manager value=(mut item.schema) choices=item.ownerSample.spider.project.schemas selecting=(mut selecting) onChange=(action 'changeSchema') create=(action 'addSchema') as |options|}}
    {{#if (eq options.section 'change-header')}}
        类型更改数据格式
    {{else if (eq options.section 'choices-header')}}
        选择一个现有的数据格式
    {{else if (eq options.section 'choice')}}
        {{list-item-icon class="icon" icon='schema'}}{{options.choice.name}}
    {{/if}}
{{/list-item-relation-manager}}
