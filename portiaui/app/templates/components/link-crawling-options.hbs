<h3>抽取规则</h3>
<form {{action "save" on="change"}}>
    {{#if (eq spider.linksToFollow 'all')}}
        <div class="text-muted">跟踪所有遇到的链接在相同的域</div>
    {{else if (eq spider.linksToFollow 'none')}}
        <div class="text-muted">不遵循任何链接</div>
    {{else if (eq spider.linksToFollow 'patterns')}}
        <div>遵循与这些模式匹配的链接</div>
        {{regex-pattern-list list=spider.followPatterns onChange=(action 'save')}}
        <div>排除与这些模式匹配的链接</div>
        {{regex-pattern-list list=spider.excludePatterns onChange=(action 'save')}}
    {{/if}}

    <div class="checkbox">
        <label>
            {{input type="checkbox" name="respectNoFollow" checked=(mut spider.respectNoFollow) disabled=(eq spider.linksToFollow 'none')}} 遵循“nofollow”属性
        </label>
    </div>
</form>
